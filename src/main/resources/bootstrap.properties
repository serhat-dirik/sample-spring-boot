spring.application.name=${project.artifactId}
spring.cloud.kubernetes.config.name=application-config
spring.cloud.kubernetes.reload.enabled=true
spring.cloud.kubernetes.secrets.paths=/etc/secrets/application-config
